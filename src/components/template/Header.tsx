/**
 * Шапка в шаблоне
 */
import * as React from 'react';

/**
 * Шапка
 */
export class Header extends React.Component {
  public render(): React.ReactElement<{}> {
    return (<section className="template__header">header</section>);
  }
}
